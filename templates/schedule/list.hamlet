<h1>Schedules
#{groupName entity}
$if null schedules
    <p>No Schedules...
$else
    <ul>
        $forall Entity scheduleId schedule <- schedules
            <li>
                #{show $ scheduleDay schedule}
<a href=@{ScheduleCreateR groupId}>create
